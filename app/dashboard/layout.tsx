import type {Metadata} from "next";
export const metadata:Metadata={title:"Supervisor dashboard preview",description:"Review clearly labelled sample worker status and real SafeSignal session records stored on this device."};
export default function DashboardLayout({children}:{children:React.ReactNode}){return children}