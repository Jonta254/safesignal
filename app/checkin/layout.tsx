import type {Metadata} from "next";
export const metadata:Metadata={title:"Start a lone-worker check-in",description:"Prepare, run, recover and export a timestamp-based lone-worker check-in session on this device."};
export default function CheckInLayout({children}:{children:React.ReactNode}){return children}