export type ProductionSessionState="draft"|"ready"|"active"|"due-soon"|"grace"|"overdue"|"escalating"|"acknowledged"|"resolved"|"completed"|"cancelled"|"expired";
const transitions:Record<ProductionSessionState,readonly ProductionSessionState[]>={draft:["ready","cancelled"],ready:["active","cancelled","expired"],active:["due-soon","grace","completed","cancelled"],"due-soon":["active","grace","completed","cancelled"],grace:["active","overdue","completed","cancelled"],overdue:["active","escalating","acknowledged","resolved"],escalating:["active","acknowledged","resolved"],acknowledged:["resolved","active"],resolved:["completed"],completed:[],cancelled:[],expired:[]};
export function canTransition(from:ProductionSessionState,to:ProductionSessionState){return transitions[from].includes(to)}
export function assertTransition(from:ProductionSessionState,to:ProductionSessionState){if(!canTransition(from,to))throw new Error("Invalid session state transition.")}
export function serverDeadlines(serverNow:Date,intervalSeconds:number,graceSeconds:number){if(!Number.isInteger(intervalSeconds)||intervalSeconds<60||intervalSeconds>86400)throw new Error("Invalid interval.");if(!Number.isInteger(graceSeconds)||graceSeconds<0||graceSeconds>3600)throw new Error("Invalid grace period.");const nextCheckInAt=new Date(serverNow.getTime()+intervalSeconds*1000);return {nextCheckInAt,graceDeadlineAt:new Date(nextCheckInAt.getTime()+graceSeconds*1000)}}